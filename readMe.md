Install required packages using beow command :
npm -i nodemon cors express firebase

To run the project use below command :
nodemon index.js
and then run 
index.html

If execution policy error occurred then set execution policy as unrestricted.
